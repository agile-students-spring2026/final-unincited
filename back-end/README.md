# Backend - News Bias App


## Project Structure
```text
back-end/
  routes/               # API routes
    analyze.js          # logic for bias/sentiment analysis 
    articles.js         # CRUD articles in collection
    auth.js             # authentication routes (login, register, reset)
  app.js                # middleware and route mounting
  server.js             # starts the server
```